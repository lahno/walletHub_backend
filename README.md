# wallet-hub-backend

#### 1. для зависимостей использовать pip-tools
- `pip-compile requirements.in --upgrade`
- `pip install -r requirements.txt`
- `pip list --outdated` - просмотреть, какие обновления доступны (без установки)
